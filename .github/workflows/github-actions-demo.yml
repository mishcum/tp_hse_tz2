name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  run-tests:
    name: Testing
    runs-on: ubuntu-latest
    steps:
      

      - name: start
        run: |
          cd demo
          mvn test


