name: GitHub Actions
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  run-tests:
    name: Testing
    runs-on: macos-latest
    steps:
      - name: start
        run: |
          mvn -f /Users/runner/work/tp_hse_tz2/tp_hse_tz2/pom.xml test


